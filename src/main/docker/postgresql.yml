version: '2'
services:
  hugoisep-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/hugoISEP/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=hugoISEP
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
